const normalizer = require('./normalizer');
const deduplicator = require('./deduplicator');
const validator = require('./validator');
const storage = require('./storage');

module.exports = { normalizer, deduplicator, validator, storage };
